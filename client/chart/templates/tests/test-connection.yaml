apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "f5oclock-client.fullname" . }}-test-connection"
  labels:
{{ include "f5oclock-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "f5oclock-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
