{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "f5oclock-client.serviceAccountName" . }}
  labels:
{{ include "f5oclock-client.labels" . | nindent 4 }}
{{- end -}}
