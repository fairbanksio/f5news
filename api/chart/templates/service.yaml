apiVersion: v1
kind: Service
metadata:
  name: {{ include "f5oclock-api.fullname" . }}
  labels:
    {{- include "f5oclock-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "f5oclock-api.selectorLabels" . | nindent 4 }}
